/**
 * Content collections.
 *
 * The schemas live in `src/schemas/` and are imported here and by the integrity
 * scripts, so every rule is defined once and read by both consumers. Collections
 * are registered as each content type is built out.
 */

export const collections = {};
